# Build per-trial basis regressors (each T x r) from SBHM basis + design_spec

Build per-trial basis regressors (each T x r) from SBHM basis +
design_spec

## Usage

``` r
.sbhm_build_trial_regs(sbhm, design_spec)
```
