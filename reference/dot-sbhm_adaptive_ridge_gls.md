# Adaptive fractional ridge per voxel from conditioning

Adaptive fractional ridge per voxel from conditioning

## Usage

``` r
.sbhm_adaptive_ridge_gls(kappa_vec, base = 0.02, k0 = 1000, max_lam = 0.08)
```
