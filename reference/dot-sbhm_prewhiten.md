# Optionally prewhiten Y, regs (stacked), intercept and nuisance

Optionally prewhiten Y, regs (stacked), intercept and nuisance

## Usage

``` r
.sbhm_prewhiten(Y, regs, Zint, Nuisance, prewhiten)
```
