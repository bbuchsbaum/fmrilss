% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lss_sbhm_design.R
\name{lss_sbhm_design}
\alias{lss_sbhm_design}
\title{SBHM Pipeline with fmridesign Models}
\usage{
lss_sbhm_design(
  Y,
  sbhm,
  event_model,
  baseline_model = NULL,
  prewhiten = NULL,
  prepass = list(),
  match = list(),
  oasis = list(),
  amplitude = list(),
  return = c("amplitude", "coefficients", "both"),
  validate = TRUE,
  ...
)
}
\arguments{
\item{Y}{Numeric matrix T×V of fMRI time series (timepoints × voxels).}

\item{sbhm}{SBHM object as returned by \code{sbhm_build()}.}

\item{event_model}{An \code{event_model} from \code{fmridesign::event_model()} defining
the trial structure (typically created with \code{trialwise()}).}

\item{baseline_model}{Optional \code{baseline_model} from
\code{fmridesign::baseline_model()}. Its drift, block, and nuisance terms are
projected out as confounds.}

\item{prewhiten}{Optional prewhitening options (see \code{?lss}).}

\item{prepass}{Optional list forwarded to \code{sbhm_prepass()}.}

\item{match}{Optional list forwarded to \code{sbhm_match()}.}

\item{oasis}{Optional SBHM OASIS override list. Supported fields are
\code{ridge_mode}, \code{ridge_x}, \code{ridge_b}, and \code{block_cols}; false-valued
\code{return_se} and \code{return_diag} are accepted, while true values fail because
SBHM uncertainty/design diagnostics are not exposed. The basis rank,
trial count, intercept policy, and trial/basis map are fixed internally
from \code{sbhm} and the event model.}

\item{amplitude}{Amplitude options (see \code{?lss_sbhm}).}

\item{return}{One of \code{"amplitude"}, \code{"coefficients"}, or \code{"both"}.}

\item{validate}{Logical; when TRUE, performs basic checks (sampling frame
compatibility, temporal alignment) analogous to \code{lss_design()}.}

\item{...}{Reserved for future use.}
}
\value{
Same return contract as \code{lss_sbhm()}.
}
\description{
Run the SBHM end-to-end pipeline using fmridesign's \code{event_model} and
optional \code{baseline_model}, mirroring the convenience of \code{lss_design()} but
producing SBHM shape coefficients and (optionally) scalar event-design trial
coefficients in the component named \code{amplitude}.
}
\details{
This function wraps \code{lss_sbhm()} by locating the unique trialwise event term,
rebuilding it with the SBHM basis, and retaining run identities and
heterogeneous durations. Non-trial event terms and \code{baseline_model} columns
enter the fixed nuisance span. Multi-run regressors are constructed within
runs so HRF tails cannot cross run boundaries.
}
\examples{
\dontshow{if (requireNamespace("fmridesign", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
  library(fmridesign)
  sframe <- fmrihrf::sampling_frame(blocklens = 60, TR = 1)
  trials <- data.frame(onset = c(5, 20, 35), run = 1)
  emod <- event_model(onset ~ trialwise(basis = "spmg1"), data = trials,
                      block = ~run, sampling_frame = sframe)
  times <- fmrihrf::samples(sframe, global = TRUE)
  H <- cbind(exp(-times / 5), exp(-times / 7))
  sbhm <- sbhm_build(library_H = H, r = 2, sframe = sframe,
                     normalize = TRUE, baseline = NULL)
  Y <- matrix(rnorm(60 * 4), 60, 4)
  out <- lss_sbhm_design(Y, sbhm, emod)
}
\dontshow{\}) # examplesIf}
}
