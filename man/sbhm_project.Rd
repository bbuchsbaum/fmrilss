% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sbhm_project.R
\name{sbhm_project}
\alias{sbhm_project}
\title{Project Trial-wise SBHM Coefficients to Scalar Amplitudes}
\usage{
sbhm_project(beta_rt, alpha_hat)
}
\arguments{
\item{beta_rt}{3D array of shape r × ntrials × V containing per-trial
coefficients in the SBHM basis (as returned by OASIS with K=r, reshaped).}

\item{alpha_hat}{Numeric matrix r×V of matched library coordinates per voxel
(e.g., `sbhm_match()$alpha_hat`). These should be in the same coordinate
system as `beta_rt` (unwhitened, not L2-normalized) for interpretable amplitudes.}
}
\value{
Numeric matrix ntrials × V of scalar amplitudes.
}
\description{
Given trial-wise coefficients in the shared basis (r×ntrials×V) and the
voxel-specific matched library coordinates `alpha_hat` (r×V), compute scalar
amplitudes per trial and voxel via inner products.
}
\examples{
\dontrun{
  # beta_mat returned by lss(..., method="oasis") with K=r has (r*ntrials) × V rows
  r <- nrow(alpha_hat)
  ntrials <- nrow(beta_mat) / r
  beta_rt <- array(beta_mat, dim = c(r, ntrials, ncol(beta_mat)))
  amps <- sbhm_project(beta_rt, alpha_hat)
}

}
