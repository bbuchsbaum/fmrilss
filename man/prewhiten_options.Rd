% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{prewhiten_options}
\alias{prewhiten_options}
\title{Construct prewhitening options}
\usage{
prewhiten_options(
  method = c("none", "ar", "arma"),
  p = "auto",
  q = 0L,
  p_max = 6L,
  pooling = c("global", "voxel", "run", "parcel"),
  runs = NULL,
  parcels = NULL,
  exact_first = c("ar1", "none"),
  compute_residuals = TRUE,
  design = NULL,
  acvf_correction = NULL,
  correction_max_lag = 25L
)
}
\arguments{
\item{method}{\code{"none"}, \code{"ar"}, or \code{"arma"}.}

\item{p}{AR order or \code{"auto"}.}

\item{q}{MA order for ARMA.}

\item{p_max}{Maximum AR order when \code{p="auto"}.}

\item{pooling}{\code{"global"}, \code{"voxel"}, \code{"run"}, or \code{"parcel"}.}

\item{runs}{Optional complete exact-integer run identifiers. Required for
\code{pooling = "run"}; execution requires one value per timepoint.}

\item{parcels}{Optional complete exact-integer parcel identifiers. Required
for \code{pooling = "parcel"}; execution requires one value per voxel.}

\item{exact_first}{\code{"ar1"} or \code{"none"}.}

\item{compute_residuals}{Logical.}

\item{design}{Optional numeric design matrix whose projection produced the
residuals used to estimate the noise model. Supplying it opts in to
fmriAR's residual-autocovariance bias correction. It must represent the
same column space as the design used for residualization.}

\item{acvf_correction}{Optional bias-correction matrix, or list of matrices,
produced by \code{fmriAR::acvf_bias_matrix()}. This is a cached alternative to
\code{design}; the two options are mutually exclusive.}

\item{correction_max_lag}{Positive integer lag budget used when \code{design} is
supplied. See \code{fmriAR::fit_noise()} for the computational and filtering
requirements of the correction.}
}
\value{
A list with class \code{"fmrilss_prewhiten_options"}.
}
\description{
Convenience constructor for the \verb{prewhiten=} list accepted by \code{lss()}.
}
\examples{
prewhiten_options(method = "ar", p = 1, pooling = "run",
                  runs = rep(1:2, each = 50))
}
