% Generated by roxygen2: do not edit by hand
% Please edit documentation in bench/stglmnet_vs_fmrilss_harness.R
\name{benchmark_stglmnet_vs_fmrilss}
\alias{benchmark_stglmnet_vs_fmrilss}
\title{Benchmark stglmnet vs fmrilss on single-trial beta recovery}
\usage{
benchmark_stglmnet_vs_fmrilss(
  n_reps = 30,
  seed = 20260227L,
  n_tp = 150L,
  n_trials = 40L,
  n_vox = 24L,
  isi = 1.2,
  tr = 1,
  noise_sd = 1.2,
  noise_model = c("iid", "ar1"),
  ar_rho = 0.35,
  beta_sparsity = 0,
  basis = "spmg1",
  onset_jitter_sd = 0,
  alpha = 0.2,
  lambda = exp(seq(log(0.2), log(0.005), length.out = 25)),
  overlap_adaptive = TRUE,
  st_configs = NULL,
  primary_st = NULL,
  cv_folds = 5L,
  cv_type.measure = c("auto", "mse", "correlation", "reliability"),
  lss_method = "r_optimized",
  lss_prewhiten = NULL,
  include_oasis = FALSE,
  oasis_grid = NULL,
  oasis_prewhiten = lss_prewhiten,
  n_runs = 1L,
  trace = FALSE
)
}
\arguments{
\item{n_reps}{Number of Monte Carlo repetitions.}

\item{seed}{Integer seed for reproducibility.}

\item{n_tp}{Number of timepoints per run (scalar) or vector across runs.}

\item{n_trials}{Number of trial regressors.}

\item{n_vox}{Number of response columns (voxels/signals).}

\item{isi}{Mean inter-stimulus interval (seconds).}

\item{tr}{Repetition time in seconds.}

\item{noise_sd}{Observation noise standard deviation.}

\item{noise_model}{Noise process: \code{"iid"} or \code{"ar1"}.}

\item{ar_rho}{AR(1) coefficient used when \code{noise_model="ar1"}.}

\item{beta_sparsity}{Fraction of trial betas set to exactly zero.}

\item{basis}{HRF basis used for trialwise design construction.}

\item{onset_jitter_sd}{Standard deviation of onset jitter (seconds).}

\item{alpha}{Elastic-net mixing parameter for \pkg{stglmnet} candidates.}

\item{lambda}{Optional lambda path for \pkg{stglmnet} candidates.}

\item{overlap_adaptive}{Backward-compatible default overlap toggle for
\code{st_configs} when not explicitly provided.}

\item{st_configs}{Optional named list of \pkg{stglmnet} argument lists.
Each config may contain optional \code{cv_args}.}

\item{primary_st}{Name of the stglmnet method to use for compatibility
\code{metrics}/\code{summary} fields. Defaults to first \code{st_configs}.}

\item{cv_folds}{Number of folds for \code{stglmnet::cv.stglmnet(...)}.}

\item{cv_type.measure}{CV objective for stglmnet candidates.}

\item{lss_method}{Method passed to \code{lss()} for the LSS baseline.}

\item{lss_prewhiten}{Optional \code{prewhiten} list passed to \code{lss()}.}

\item{include_oasis}{If \code{TRUE}, evaluate OASIS over \code{oasis_grid}.}

\item{oasis_grid}{Optional data frame with columns
\code{ridge_mode}, \code{ridge_x}, and \code{ridge_b}.}

\item{oasis_prewhiten}{Optional \code{prewhiten} list for OASIS; defaults to
\code{lss_prewhiten}.}

\item{n_runs}{Number of runs.}

\item{trace}{If \code{TRUE}, prints per-repetition progress.}
}
\value{
A list with compatibility fields (\code{metrics}/\code{summary}) plus
method-wise long-format benchmark outputs.
}
\description{
Simulates overlapping trial-wise fMRI designs with known trial betas, then
compares recovery accuracy between \pkg{stglmnet} variants and
\code{fmrilss::lss()} (optionally including OASIS parameter grids).
}
