% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/item_slice_fold.R
\name{item_slice_fold}
\alias{item_slice_fold}
\title{Slice an ITEM bundle into train/test fold objects}
\usage{
item_slice_fold(bundle, test_run, check_hash = FALSE)
}
\arguments{
\item{bundle}{Object of class \code{item_bundle}.}

\item{test_run}{Run/session id to hold out for testing.}

\item{check_hash}{Logical; if \code{TRUE}, validate stored \code{trial_hash}.}
}
\value{
A list with train/test slices:
\code{Gamma_train}, \code{Gamma_test}, \code{T_train}, \code{T_test},
\code{U_train}, \code{U_test}, \code{train_idx}, \code{test_idx}, \code{train_runs}, \code{test_run}.
}
\description{
Create deterministic leave-one-run-out slices for \code{Gamma}, \code{T_target}, and
trial covariance (\code{U} or \code{U_by_run}).
}
\examples{
bundle <- item_build_design(
  X_t = diag(4),
  T_target = factor(c("A", "B", "A", "B")),
  run_id = c(1, 1, 2, 2)
)
bundle$Gamma <- matrix(c(1, 0, 0.8, 0.2, 0.2, 0.8, 0, 1), 4, 2, byrow = TRUE)
bundle$U <- diag(4)
fold <- item_slice_fold(bundle, test_run = 2)
fold$test_idx

}
