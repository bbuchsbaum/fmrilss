% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lss_sbhm.R
\name{lss_sbhm}
\alias{lss_sbhm}
\title{End-to-End LSS with Shared-Basis HRF Matching (SBHM)}
\usage{
lss_sbhm(
  Y,
  sbhm,
  design_spec,
  Nuisance = NULL,
  prewhiten = NULL,
  prepass = list(),
  match = list(),
  oasis = list(),
  amplitude = list(),
  return = c("amplitude", "coefficients", "both")
)
}
\arguments{
\item{Y}{Numeric matrix T×V of fMRI time series.}

\item{sbhm}{SBHM object from \code{sbhm_build()}.}

\item{design_spec}{List for design construction:
\code{list(sframe=..., cond=list(onsets=..., duration=0, span=...), others=list(...))}.
The target HRF is replaced by the SBHM basis. Multi-run inputs use
run-relative onsets and an explicit \code{cond$run} vector; other conditions use
the same event fields and may supply their own HRF.}

\item{Nuisance}{Optional T×P nuisance regressors.}

\item{prewhiten}{Optional prewhitening options (see \code{?lss}). Run labels are
inferred from \code{design_spec$sframe} when omitted; supplied labels must match
those sampling-frame boundaries.}

\item{prepass}{Optional list forwarded to \code{sbhm_prepass()} (e.g., ridge, data_fac).}

\item{match}{Optional list forwarded to \code{sbhm_match()} (e.g., shrink, topK, whiten, orient_ref).
Additional fields handled here:
\itemize{
\item \code{alpha_source}: one of \code{"prepass"} (default), \code{"trial_projection"},
or \code{"oasis_rank1"}.
\code{"trial_projection"} estimates voxel shape from per-trial projection
coefficients in the shared basis.
\item \code{rank1_min}: optional minimum rank-1 variance fraction in \verb{[0,1]} when
\code{alpha_source="oasis_rank1"}. Voxels below threshold fall back to prepass.
\item \code{soft_blend} logical (default TRUE): when \code{topK > 1}, blend the top-K
library coordinates per voxel using softmax weights returned by \code{sbhm_match()}.
If \code{blend_margin} is provided, blending is only applied to voxels with
\code{margin < blend_margin}; others use the hard top-1 assignment.
\item \code{blend_margin} optional numeric threshold on the matching margin for
conditional blending.
\item \code{whiten_power} numeric in \verb{[0,1]} for partial singular-value whitening
(\code{1}=full, \code{0.5}=partial).
\item \code{min_margin} optional minimum matching margin. Voxels below threshold
fall back to \code{fallback_ref}.
\item \code{min_beta_norm} optional minimum norm of the shape summary used for
matching. Voxels below threshold fall back to \code{fallback_ref}.
\item \code{fallback_ref} optional r-vector fallback coordinate (default
\code{sbhm$ref$alpha_ref}).
}}

\item{oasis}{Optional list forwarded to \code{lss(..., method="oasis")}. The basis
dimension, trial count, run-specific intercept span, and trial/basis map
are supplied from the run-safe SBHM design.}

\item{amplitude}{List controlling the scalar amplitude stage. Fields:
\itemize{
\item \code{method}: one of "lss1" (default), "global_ls", "oasis_voxel".
\item \code{ridge}: for \code{global_ls}, either numeric (absolute) or list(mode, lambda).
\item \code{ridge_frac}: for \code{lss1}/\code{oasis_voxel}, list(x, b) fractional ridge.
\item \code{cond_gate}: optional auto-fallback rule, e.g., list(metric="rho", thr=0.999, fallback="lss1").
}}

\item{return}{One of \code{"amplitude"}, \code{"coefficients"}, or \code{"both"} (default \code{"amplitude"}).}
}
\value{
A list with components:
\itemize{
\item \code{amplitude} ntrials×V matrix (when requested)
\item \code{coeffs_r} r×ntrials×V array of trial-wise coefficients (when requested)
\item \code{matched_name} and \code{matched_idx}: named top-scoring library identities
\item \code{margin} named length-V score differences (top1 - top2 cosine); these are
not calibrated confidence measures
\item \code{alpha_coords} r×V matched coordinates per voxel
\item \code{shape_mode} and \code{fallback_low_conf}: named vectors recording the shape
policy actually used
\item \code{prepass_fallback}: named logical vector identifying voxels whose requested
shape source fell back to the aggregate prepass
\item \code{trial_basis_map}: complete trial/basis output identity
\item \code{event_amplitude}, \code{event_duration}, and \code{event_run}: named event-design
metadata
\item \code{diag} list with \code{r}, \code{ntrials}, and \code{times}
}

Scalar amplitudes are coefficients on the supplied event design using the
selected rank-truncated library shape. They are not automatically peak BOLD
responses. SBHM standard errors are not calibrated and \code{return_se=TRUE}
fails explicitly for every amplitude method.
}
\description{
Orchestrates the SBHM pipeline: (1) run-safe trial design and OASIS fit in the
shared basis, (2) a voxel shape summary from the requested source, (3) hard
library matching or explicit blending, and (4) a separate scalar-coefficient
refit using the selected voxel shape.
}
\details{
Most users should treat the \code{prepass}, \code{match}, \code{oasis}, and \code{amplitude} inputs
as optional nested \emph{override lists}: you can provide only the fields you want
to change. Unspecified nested defaults are preserved.

If you already use \code{fmridesign}, prefer \code{\link[=lss_sbhm_design]{lss_sbhm_design()}} to avoid manually
assembling an OASIS \code{design_spec}.
}
\examples{
\donttest{
  library(fmrihrf)
  set.seed(3)
  Tlen <- 180; V <- 4
  sframe <- sampling_frame(blocklens = Tlen, TR = 1)
  H <- cbind(exp(-seq(0, 30, length.out = Tlen)/5),
             exp(-seq(0, 30, length.out = Tlen)/7))
  sbhm <- sbhm_build(library_H = H, r = 2, sframe = sframe, normalize = TRUE)
  onsets <- seq(8, 140, by = 12)
  design_spec <- list(sframe = sframe, cond = list(onsets = onsets, duration = 0, span = 30))
  hrf_B <- sbhm_hrf(sbhm$B, sbhm$tgrid, sbhm$span)
  rr <- fmrihrf::regressor(onsets = onsets, hrf = hrf_B, duration = 0, span = 30, summate = FALSE)
  Xr <- fmrihrf::evaluate(rr, grid = sbhm$tgrid, precision = 0.1, method = "conv")
  alpha_true <- rnorm(ncol(sbhm$B))
  Y <- matrix(rnorm(Tlen*V, sd = .6), Tlen, V)
  Y[,1] <- Y[,1] + Xr \%*\% alpha_true
  out <- lss_sbhm(Y, sbhm, design_spec)
  out2 <- lss_sbhm(Y, sbhm, design_spec,
                  match = list(topK = 2, soft_blend = TRUE),
                  return = "amplitude")
  names(out)
}

}
\seealso{
\code{\link[=lss_sbhm_design]{lss_sbhm_design()}}, \code{\link[=sbhm_prepass]{sbhm_prepass()}}, \code{\link[=sbhm_match]{sbhm_match()}}
}
